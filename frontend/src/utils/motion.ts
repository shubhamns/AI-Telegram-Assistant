import type { Transition } from "framer-motion";
export const easeOut: Transition = { duration: 0.22, ease: [0.25, 0.1, 0.25, 1] };
export const scaleTap = { whileTap: { scale: 0.97 }, transition: { duration: 0.12 } };
